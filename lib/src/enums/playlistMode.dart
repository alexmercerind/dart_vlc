/// Enum to specify the mode of [Playlist].
enum PlaylistMode {
  /// Indicate [single] [Playlist] mode
  single,
  /// Indicate [loop] [Playlist] mode
  loop,
  /// Indicate [repeat] [Playlist] mode
  repeat
}